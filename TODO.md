# Life Journal Todo List

## 照片、视频与相册

**状态：** 计划中

Life Journal 最初强调用一两句话完成记录，避免为了写日记投入过多时间。这个原则不变。但照片和视频确实能保存一些文字无法完整表达的重要时刻，因此后续会把媒体附件作为可选能力加入，而不是变成每天必须完成的记录步骤。

### 设计原则

- 先完成文字记录，再按需要补充附件。
- 图片和视频是可选内容，不鼓励每篇日记大量插入。
- 原始文件保存在本地目录中，Markdown 只保存相对路径和必要元数据。
- 不把图片转成 Base64 写进 Markdown，也不默认上传到第三方服务。
- 图片、视频必须能追溯到具体日期和具体事件。
- 公开演示继续使用虚构素材，发布检查不得包含真实私人媒体。

### 图片与视频

- [ ] 在 `life.config.yml` 中增加附件目录配置。
- [ ] 设计稳定的事件标识，解决同一天存在多个事件时的精确关联问题。
- [ ] 建立按年份、日期和事件组织的附件目录，例如 `attachments/2026/2026-09-01/event-id/`。
- [ ] 增加附件索引，保存相对路径、日期、事件、说明、文件类型和创建时间。
- [ ] 支持用户通过 Agent 指定“把这张照片或视频关联到哪一天的哪件事”。
- [ ] 当日期或事件不明确时，由 Agent 询问确认，不自动猜测。
- [ ] 保留原始图片和视频，并生成适合网页浏览的图片缩略图和视频封面。
- [ ] 读取视频时长、尺寸等基础信息，并支持常见本地视频格式。
- [ ] 在日记详情页展示与当天事件关联的图片和视频。
- [ ] 新增相册页面，支持按年份、月份、日期和事件浏览。
- [ ] 支持从相册跳回对应日记和事件。
- [ ] 在日记详情和相册页面中提供视频播放入口。
- [ ] 对大文件给出提示，第一版不内置复杂的视频转码和编辑功能。

### 安全与维护

- [ ] 清理日记事件时检查附件是否成为孤立文件，但不自动删除原始媒体。
- [ ] 增加附件路径、重复文件、缺失文件和断链检查。
- [ ] 说明附件目录的备份、同步和迁移方式。
- [ ] 发布检查排除真实照片、视频、缩略图缓存和本机绝对路径。
- [ ] 为目录解析、事件关联、相册生成和隐私检查增加测试。

## 私有访问控制

**状态：** 计划中，第一版暂不内置

当前 Web 阅读界面没有密码或登录功能。第一版建议只在本机、NAS 私有局域网或 Tailscale/VPN 中访问，不直接暴露到公网。后续只有在以下安全条件都能满足时，才考虑加入内置访问控制：

- [ ] 登录必须由服务端校验，不能只在前端隐藏页面。
- [ ] 只保存 Argon2id 等强算法生成的密码哈希，不保存明文密码。
- [ ] 密码哈希、会话密钥等敏感值只能来自私密环境变量。
- [ ] 使用 `HttpOnly`、`Secure`、`SameSite` Cookie 保存短期会话。
- [ ] 支持会话过期、主动退出和服务端撤销。
- [ ] 对登录尝试进行限速，并对连续失败执行临时锁定。
- [ ] 远程访问必须使用 HTTPS，不能在明文 HTTP 中提交密码。
- [ ] 增加认证绕过、Cookie、安全响应头、暴力尝试和错误配置测试。
- [ ] 文档明确区分局域网模式、VPN 模式和公网反向代理模式。

在内置认证完成前，推荐使用 Tailscale/VPN，或由成熟的反向代理和身份认证服务保护 Web 阅读界面。

## 英文与国际化

**状态：** 后续评估，首个公开版本保持中文优先

当前前端文案、Demo 数据、字段示例和测试都以中文为中心。首个公开版本先提供简短英文介绍，不立即维护一套不完整的英文界面。后续如有稳定的英文用户需求，再统一完成：

- [ ] 为宣传页和 Web 阅读界面建立正式的 i18n 文案层。
- [ ] 提供英文 Demo Vault，不与中文 Demo 混用字段和内容。
- [ ] 翻译 Skill 参考文档和 Vault 模板，并确保中英文规则语义一致。
- [ ] 明确日期、农历、星期、媒体分类和地点字段的本地化策略。
- [ ] 为中英文页面、路由和元数据增加自动化测试。
- [ ] 在发布流程中检查两种语言是否同步，避免只更新其中一套。

## 发布与更新

**状态：** 首版实现中

- [x] Skill 安装目录与用户 Vault 完全分离。
- [x] 将完整记录规则统一收入 Skill，Vault 只保存数据与结构化配置。
- [x] 增加 `schema_version`，支持只读结构版本检查。
- [x] 增加 Codex Plugin 与仓库 Marketplace 包装。
- [ ] GitHub 仓库发布后，在 README 中写入准确的安装与更新命令。
- [ ] 首次需要迁移时，新增逐版本迁移说明和可审阅的差异流程。
- [x] 使用跨 Agent 的 `npx skills` 作为主要安装与更新方式。

### 暂不计划

- 云端相册托管。
- 人脸识别和人物自动标注。
- 图片美化、滤镜或复杂编辑。
- 面向社交平台的公开分享流程。
- 鼓励为了排版和配图增加日记记录负担。
